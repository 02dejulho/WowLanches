@model LancheListViewModel 


<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="row">

        @foreach(var lanche in Model.Lanches) //foreach percorrerá toda a coleção de lanches e os exibirá na lista
        {
            <div class="col-sm-4 col-md-4 col-lg-4">
                <div>
                    <p><img src="@lanche.ImagemUrl" class="img-flluid"></p>                               
                    <h4>@lanche.Nome</h4>
                    <h5>Preço : @lanche.Preco.ToString("c")</h5> 
                    <p>@lanche.DescricaoCurta</p>
                </div>
            </div>
        }
    using WowLanches.ViewModels;

    </div>
</div>