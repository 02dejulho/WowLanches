@model Lanche

<div class="col-sm-4 col-md-4 col-lg-4">
    <div>
        <p><img src="@Model.ImagemUrl" class="img-flluid"></p>                               
        <h4>@Model.Nome</h4>
        <h5>@Model.Preco.ToString("c")</h5> 
        <p>@Model.DescricaoCurta</p>
    </div>
</div>